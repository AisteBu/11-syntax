
parasyti funkcija:
- duoto zodzio pirma raide
- duoto zodzio vidurine raide
- duoto zodzio paskutine raide
- ar duoto zodzio N-toji raide yra didzioji? true / false
- ar duoto zodzio N-toji raide yra mazoji? true / false

1. iprasta funkcija
2. konstantai priskirta anonimine funkcija
3. rodykline funkcija
4. jei logikoje tik viena operacija, galime istrinti {} ir return
5. jei gauname tik viena parametra, galime istrinti ()

/*

1. const pirmaRaide = text => text[a];
}

2. const vidurineRaide = text => {
    if (text === '') {
        return '';
    }
    const index = Math.ceil(text.length / 2 - 1);

    return text[index];
}

3. const paskutineRaide = text => text[text.length - 1];
}

4. const Nraide = text => {
    if (Nraide === uppercase) {
        return 'true';
    }
    return 'false';
}

5. const Nraide = text => {
    if (Nraide === undercase) {
        return 'true';
    }
    return 'false';
}

*/ 
